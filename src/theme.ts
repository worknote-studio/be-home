import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#d9d0c2",
    },
    secondary: {
      main: "#989488",
    },
    background: {
      default: "#fafafa",
    },
  },
});

export default theme;
